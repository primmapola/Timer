# Инструкция по настройке Live Activity

## ✅ Что уже сделано

1. ✅ Создан `BoxingTimerActivityAttributes.swift` - модель данных
2. ✅ Создан `TimerLiveActivity.swift` - UI для Live Activity
3. ✅ Обновлен `BoxingTimerModel.swift` - интеграция с Activity
4. ✅ Настроен `TimerWidgetExtensionBundle.swift`
5. ✅ Проект успешно компилируется

## 📝 Что нужно сделать в Xcode

### Шаг 1: Добавить BoxingTimerActivityAttributes в оба targets

1. В Project Navigator найдите файл `Timer/Models/BoxingTimerActivityAttributes.swift`
2. Выберите его
3. В правой панели (File Inspector) найдите секцию **Target Membership**
4. Поставьте галочки для обоих targets:
   - ☑️ Timer
   - ☑️ TimerWidgetExtensionExtension

### Шаг 2: Добавить NSSupportsLiveActivities в Info.plist

1. Выберите target **Timer** (не Widget Extension!)
2. Перейдите на вкладку **Info**
3. Нажмите на **+** рядом с любым ключом
4. Введите: `NSSupportsLiveActivities`
5. Убедитесь, что Type = Boolean
6. Установите Value = **YES**

### Шаг 3: Запустить приложение

1. Выберите схему **Timer** (Cmd+<)
2. Выберите симулятор или реальное устройство
3. Запустите приложение (Cmd+R)
4. Нажмите кнопку **Старт**
5. Live Activity должна появиться:
   - На экране блокировки (заблокируйте устройство)
   - В Dynamic Island (только iPhone 14 Pro+ на реальном устройстве)

## 🎯 Что показывает Live Activity

### Экран блокировки:
- Текущая фаза: РАУНД (красный) или ОТДЫХ (зеленый)
- Номер раунда: "Раунд 1/3"
- Оставшееся время: "03:00"
- Статус паузы (если на паузе)

### Dynamic Island (iPhone 14 Pro+):
- **Компактный**: Иконка бокса + время
- **Расширенный**: Полная информация с таймером и раундами

## 🔍 Проверка настройки

Запустите в терминале:
```bash
# Проверить, что проект собирается
xcodebuild -scheme Timer -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build

# Должно быть: ** BUILD SUCCEEDED **
```

## ❓ Решение проблем

### Ошибка: "Cannot find 'BoxingTimerActivityAttributes' in scope"
→ Добавьте файл в оба targets (Шаг 1)

### Ошибка: "'main' attribute can only apply to one type"
→ Уже исправлено! Используется только TimerWidgetExtensionBundle

### Live Activity не появляется
→ Убедитесь, что добавили NSSupportsLiveActivities = YES (Шаг 2)

### Dynamic Island не работает
→ Это нормально в симуляторе, нужно реальное устройство iPhone 14 Pro+

## 📱 Тестирование

1. Запустите приложение
2. Нажмите **Старт**
3. Сверните приложение (свайп вверх)
4. Или заблокируйте экран
5. Live Activity должна быть видна!

## 🎉 Готово!

После выполнения этих шагов ваш бокс-таймер будет показывать Live Activity на экране блокировки во время тренировки!
